<div class="row mt-5">
    <div class="col-md-6 m-auto">
        <h1>List of all users</h1>
            <ul>
            <% data.users.forEach(function(user) { %>
                <li> email: <%= user.email %> | <%= user.firstName %></li>
            <% }); %> 
            </ul>
    </div>
</div>