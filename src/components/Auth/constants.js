const SECRET_ACCES_TOKEN = 'qwerty';
const SECRET_REFRESH_TOKEN = 'ytrewq';
const ACCES_EXPAIR_TIME = '60m';
const REFRESH_EXPAIR_TIME = '90d';

module.exports = Object.freeze({
    SECRET_ACCES_TOKEN,
    SECRET_REFRESH_TOKEN,
    ACCES_EXPAIR_TIME,
    REFRESH_EXPAIR_TIME,
});
